[project]
name = "sentinelpay-fraud-detection-api"
version = "1.2.0"
description = "Streaming-ready FastAPI fraud anomaly detection service with a human-readable analyst layer."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Mouhamedsouhail" }]
keywords = ["fraud-detection", "fastapi", "kafka", "scikit-learn", "anomaly-detection"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
